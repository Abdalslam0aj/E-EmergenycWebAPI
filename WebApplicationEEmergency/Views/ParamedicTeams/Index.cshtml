@model IEnumerable<WebApplicationEEmergency.ParamedicTeam>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.deploymentLocation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DCD.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.deploymentLocation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DCD.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.teamNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.teamNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.teamNumber })
        </td>
    </tr>
}

</table>
